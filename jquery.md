jquery is a library for javascript. It doesn't actually add any funtcionality,
but makes functions easier to write
an example is $ in jquery which is the equivalent of getElementById in plain javascript
